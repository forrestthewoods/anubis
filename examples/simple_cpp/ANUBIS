cpp_binary(
    name = "simple_cpp",
    srcs = [ "main.cpp" ],
    srcs2 = glob(["*.cpp"]),
    srcs3 = select(
        (platform, arch) => {
            (windows, x64) = ["foo"],
            (linux | macos, _) = ["bar"],
            default = ["baz"],
        }
    ),
)
